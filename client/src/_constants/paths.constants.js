import { PUBLIC_URL } from './app.constants';

export const HOME_PAGE = `${PUBLIC_URL}/`;
export const PRODUCT_PAGE = `${PUBLIC_URL}/product`;
export const CART_PAGE = `${PUBLIC_URL}/cart`;
export const NOTFOUND_PAGE = `${PUBLIC_URL}/404`;

export default {
  HOME_PAGE,
  PRODUCT_PAGE,
  CART_PAGE,
  NOTFOUND_PAGE,
};
